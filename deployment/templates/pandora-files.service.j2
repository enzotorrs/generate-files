[Unit]
Description=Pandora files backend
After=network.target

[Service]
Environment=NODE_ENV=production
WorkingDirectory={{ dest_dir }}
Type=simple
User={{ user }}
ExecStart=node app.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
